export type ProductType = {
    id: number,
    name: string,
    title: string,
    price: number | null,
    description: string | null,
    category: string,
    image: string,
    rating: number,
}